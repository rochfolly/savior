import { GraphQLObjectType, GraphQLInt, GraphQLBoolean, GraphQLString, GraphQLSchema } from 'graphql';

// Category
const CategoryType = new GraphQLObjectType({
    name: 'category',
    fields: () => {
        
    }
})

const ExpenseType = new GraphQLObjectType({
    name: 'expense',
    fields: () => {
        
    }
})